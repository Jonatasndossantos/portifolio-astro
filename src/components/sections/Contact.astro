---
import { getTranslations, useTranslator } from "@i18n";
import { Icon } from "astro-icon/components";
import type { CuratedProfile } from "../../types";

interface Props {
    profile: CuratedProfile;
    lang: string;
}

const { profile, lang } = Astro.props;
const __ = await useTranslator(lang);
---

<section id="contact" class="py-32 px-6 lg:px-20 relative z-10 overflow-hidden">
    <!-- Decorative Blob -->
    <div
        class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[500px] h-[500px] bg-primary/10 blur-[120px] rounded-[calc(var(--radius-box)*2)] pointer-events-none"
    >
    </div>

    <div class="max-w-5xl mx-auto relative z-10">
        <div class="grid md:grid-cols-2 gap-16">
            <!-- Contact Info -->
            <div>
                <h2
                    class="text-xs font-bold text-primary tracking-widest uppercase mb-4"
                >
                    // {__("Contact")}
                </h2>
                <h3
                    class="text-5xl md:text-6xl font-bold text-base-content mb-6"
                >
                    {__("Let's Build the Future.")}
                </h3>
                <p class="text-base-content/70 text-lg mb-8">
                    {__("Let's discuss how we can work together to bring your ideas to life.")}
                </p>

                <div class="flex flex-col gap-4">
                    <a
                        href={`mailto:${profile.socials.email}`}
                        class="flex flex-col sm:flex-row items-center sm:items-start gap-4 p-4 glass rounded-box hover:bg-base-content/5 transition-all group border border-base-content/10 text-center sm:text-left"
                        aria-label="Email"
                    >
                        <div
                            class="p-3 bg-base-100 rounded-[calc(var(--radius-box)*2)] text-primary group-hover:scale-110 transition-transform"
                        >
                            <Icon name="lucide:mail" class="size-6" aria-hidden="true" />
                        </div>
                        <div>
                            <div
                                class="text-xs text-base-content/50 uppercase tracking-wider"
                            >
                                {__("Email")}
                            </div>
                            <div
                                class="text-base-content font-mono font-medium"
                            >
                                {profile.socials.email}
                            </div>
                        </div>
                    </a>
                    <a
                        href={profile.socials.linkedin}
                        target="_blank"
                        rel="noreferrer"
                        class="flex flex-col sm:flex-row items-center sm:items-start gap-4 p-4 glass rounded-box hover:bg-base-content/5 transition-all group border border-base-content/10 text-center sm:text-left"
                        aria-label="LinkedIn"
                    >
                        <div
                            class="p-3 bg-base-100 rounded-[calc(var(--radius-box)*2)] text-primary group-hover:scale-110 transition-transform"
                        >
                            <Icon name="lucide:linkedin" class="size-6" aria-hidden="true" />
                        </div>
                        <div>
                            <div
                                class="text-xs text-base-content/50 uppercase tracking-wider"
                            >
                                LinkedIn
                            </div>
                            <div
                                class="text-base-content font-mono font-medium"
                            >
                                /in/jonatasndossantos
                            </div>
                        </div>
                    </a>
                    <a
                        href={profile.socials.github}
                        target="_blank"
                        rel="noreferrer"
                        class="flex flex-col sm:flex-row items-center sm:items-start gap-4 p-4 glass rounded-box hover:bg-base-content/5 transition-all group border border-base-content/10 text-center sm:text-left"
                        aria-label="GitHub"
                    >
                        <div
                            class="p-3 bg-base-100 rounded-[calc(var(--radius-box)*2)] text-primary group-hover:scale-110 transition-transform"
                        >
                            <Icon name="simple-icons:github" class="size-6" aria-hidden="true" />
                        </div>
                        <div>
                            <div
                                class="text-xs text-base-content/50 uppercase tracking-wider"
                            >
                                GitHub
                            </div>
                            <div
                                class="text-base-content font-mono font-medium"
                            >
                                jonatasndossantos
                            </div>
                        </div>
                    </a>
                </div>
            </div>

            <!-- Contact Form Component (Native Vanilla JS) -->
            <form
                id="contact-form"
                class="glass p-8 rounded-[calc(var(--radius-box)*2)] border border-base-content/10 space-y-6 relative overflow-hidden bg-base-200/50 shadow-2xl"
                data-sending-text={__("SENDING...")}
            >
                <div
                    id="form-success"
                    class="hidden absolute inset-0 bg-base-200/95 backdrop-blur-md z-20 flex-col items-center justify-center text-center p-6 sm:p-8 animate-fade-in"
                >
                    <Icon name="lucide:check-circle" class="size-16 text-success mb-4" aria-hidden="true" />
                    <h4 class="text-2xl font-bold text-base-content mb-2">
                        {__("Message Sent Successfully")}
                    </h4>
                    <p class="text-base-content/70">
                        {__("Thanks for reaching out! I'll get back to you soon.")}
                    </p>
                    <button
                        type="button"
                        id="btn-reset-form"
                        class="btn btn-outline btn-sm mt-6 rounded-[calc(var(--radius-box)*2)] uppercase tracking-widest"
                    >
                        {__("Send Another Message")}
                    </button>
                </div>

                <div>
                    <label
                        for="name"
                        class="block text-xs uppercase font-bold text-base-content/60 mb-2"
                        >{__("Name")}</label
                    >
                    <input
                        required
                        name="name"
                        id="name"
                        type="text"
                        class="input input-bordered w-full bg-base-100 focus:input-primary"
                        placeholder="John Doe"
                    />
                </div>
                <div>
                    <label
                        for="email"
                        class="block text-xs uppercase font-bold text-base-content/60 mb-2"
                        >{__("Email Address")}</label
                    >
                    <input
                        required
                        name="email"
                        id="email"
                        type="email"
                        class="input input-bordered w-full bg-base-100 focus:input-primary"
                        placeholder="john@example.com"
                    />
                </div>
                <div>
                    <label
                        for="message"
                        class="block text-xs uppercase font-bold text-base-content/60 mb-2"
                        >{__("Message")}</label
                    >
                    <textarea
                        required
                        name="message"
                        id="message"
                        class="textarea textarea-bordered w-full h-32 bg-base-100 focus:textarea-primary resize-none"
                        placeholder={__("Tell me about your project...")}></textarea>
                </div>

                <div
                    id="form-error"
                    class="hidden items-center gap-2 text-error text-sm bg-error/10 p-3 rounded-lg border border-error/20"
                >
                    <Icon name="lucide:alert-circle" class="size-5" aria-hidden="true" />
                    <span>{__("Something went wrong. Please try again.")}</span>
                </div>

                <button
                    id="submit-button"
                    type="submit"
                    class="btn btn-primary w-full tracking-widest uppercase group"
                >
                    <span id="submit-text">{__("Send Message")}</span>
                    <Icon name="lucide:send" class="size-4 group-hover:translate-x-1 transition-transform" aria-hidden="true" />
                </button>
            </form>
        </div>
    </div>
</section>

<script>
    // Handles form submission via Vanilla JS to avoid React Hydration.
    // Encapsulate inside an event listener to support Astro View Transitions if enabled.
    document.addEventListener("astro:page-load", () => {
        const form = document.getElementById("contact-form") as HTMLFormElement;
        const successOverlay = document.getElementById("form-success");
        const errorAlert = document.getElementById("form-error");
        const submitBtn = document.getElementById(
            "submit-button",
        ) as HTMLButtonElement;
        const submitText = document.getElementById("submit-text");
        const resetBtn = document.getElementById("btn-reset-form");

        if (!form) return;

        form.addEventListener("submit", async (e) => {
            e.preventDefault();

            // Disable button during flight
            submitBtn.disabled = true;
            const originalText = submitText!.innerText;
            const sendingText = form.getAttribute("data-sending-text") || "SENDING...";
            submitText!.innerText = sendingText;
            errorAlert!.classList.add("hidden");

            const formData = new FormData(form);
            const FORMSPREE_ID = "mrbnjqlz";

            try {
                const response = await fetch(
                    `https://formspree.io/f/${FORMSPREE_ID}`,
                    {
                        method: "POST",
                        body: formData,
                        headers: { Accept: "application/json" },
                    },
                );

                if (response.ok) {
                    form.reset();
                    successOverlay!.classList.remove("hidden");
                    successOverlay!.classList.add("flex");
                } else {
                    errorAlert!.classList.remove("hidden");
                    errorAlert!.classList.add("flex");
                }
            } catch (err) {
                errorAlert!.classList.remove("hidden");
                errorAlert!.classList.add("flex");
            } finally {
                submitBtn.disabled = false;
                submitText!.innerText = originalText;
            }
        });

        // Reset the form view so users can send another message
        resetBtn?.addEventListener("click", () => {
            successOverlay!.classList.add("hidden");
            successOverlay!.classList.remove("flex");
        });
    });
</script>
